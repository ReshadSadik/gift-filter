import React from "react";
import styles from "@/styles/Home.module.css";
import { Box } from "@mui/material";
const filteredResults = () => {
  return <Box className={styles.main}>filtered items</Box>;
};

export default filteredResults;
